# Extra deps layered onto the Airflow image: the dbt stack + the extract EL deps.
# Intentionally NOT pinning SQLAlchemy (Airflow 2.10 requires 1.4.x; our db.py
# works on it) or pandas (unused by the clean extract layer) to avoid clobbering
# Airflow's constrained deps. dbt packages are mounted from the host at runtime.
dbt-core>=1.10,<1.12
dbt-postgres>=1.10,<1.12
requests>=2.32
pydantic>=2.7
pydantic-settings>=2.3
tenacity>=8.4
boto3>=1.34
psycopg2-binary>=2.9
