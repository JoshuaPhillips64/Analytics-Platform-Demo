# --- Legacy reference (present on disk for Claude Code to read; NOT committed) ---
# This keeps the public portfolio repo clean. If you'd rather ship the old code
# as part of the repo, delete the next line.
_legacy_reference/

# --- secrets / env ---
.env
.env.*
!.env.example
*.pem

# --- python ---
__pycache__/
*.py[cod]
.venv/
venv/
.uv/
dist/
build/
*.egg-info/

# --- dbt ---
transform/target/
transform/dbt_packages/
transform/logs/
.user.yml

# --- airflow ---
orchestration/logs/
orchestration/plugins/__pycache__/

# --- terraform ---
# State holds the RDS master password in plaintext; tfvars holds secrets.
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
!*.tfvars.example
tfplan
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
# Keep .terraform.lock.hcl committed for reproducible provider versions.

# --- os / editor ---
.DS_Store
.idea/
.vscode/
